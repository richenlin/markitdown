# MarkItDown 离线 LLM OCR + 桌面客户端实施方案

> 文档版本：2026-04-23  
> 状态：评估阶段（代码未改动）

---

## 一、总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户桌面（任意平台）                          │
│                                                                 │
│   ┌──────────────────────┐    ┌────────────────────────────┐   │
│   │  Electron 桌面客户端  │    │  Claude / Cursor / 其他    │   │
│   │  ┌────────┐ ┌──────┐ │    │  支持 MCP 的大模型客户端   │   │
│   │  │文件拖拽│ │MD预览│ │    │                            │   │
│   │  └───┬────┘ └──────┘ │    └───────────┬────────────────┘   │
│   └───────┼───────────────┘               │ MCP (SSE/HTTP)     │
│           │ HTTP POST /api/convert        │ /sse  /mcp         │
└───────────┼───────────────────────────────┼────────────────────┘
            │                               │
            ▼       localhost:8778          ▼
┌─────────────────────────────────────────────────────────────────┐
│              Docker Compose 服务栈                               │
│                                                                 │
│  ┌──────────────────────────────────────┐   ┌───────────────┐  │
│  │       markitdown-api (port 8778)     │   │    ollama     │  │
│  │                                      │──▶│  qwen2-vl:7b  │  │
│  │  ┌─────────────┐  ┌───────────────┐  │   │  port: 11434  │  │
│  │  │  REST API   │  │  MCP Server   │  │   │  V100 16GB   │  │
│  │  │ /api/convert│  │ /sse  /mcp    │  │   └───────────────┘  │
│  │  │ /api/health │  │ /messages/    │  │                      │
│  │  └─────────────┘  └───────────────┘  │                      │
│  │     FastAPI           FastMCP         │                      │
│  │     (Starlette 合并挂载，单进程)       │                      │
│  └──────────────────────────────────────┘                      │
│                  docker network: markitdown-net                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、Part 1：Docker 服务栈

### 2.1 目录结构规划

```
markitdown/
├── Dockerfile                        # 现有（markitdown CLI，保持不变）
├── docker-compose.yml                # 新增
├── docker/
│   ├── ollama/
│   │   ├── Dockerfile                # 新增：Ollama + GPU 服务
│   │   └── entrypoint.sh             # 新增：启动 + 自动拉取模型
│   └── markitdown-api/
│       ├── Dockerfile                # 新增：API 服务镜像
│       └── app.py                    # 新增：FastAPI 包装层
```

---

### 2.2 Ollama 服务镜像

**文件：`docker/ollama/Dockerfile`**

```dockerfile
FROM ollama/ollama:latest

# 预写入模型拉取脚本（首次启动时自动执行）
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

EXPOSE 11434

HEALTHCHECK --interval=30s --timeout=10s --start-period=120s --retries=5 \
    CMD curl -f http://localhost:11434/api/tags || exit 1

ENTRYPOINT ["/entrypoint.sh"]
```

**文件：`docker/ollama/entrypoint.sh`**

```bash
#!/bin/bash
set -e

MODEL="${OLLAMA_MODEL:-qwen2-vl:7b}"

# 启动 Ollama 服务（后台）
ollama serve &
OLLAMA_PID=$!

# 等待服务就绪
echo "[entrypoint] Waiting for Ollama to start..."
until curl -sf http://localhost:11434/api/tags > /dev/null 2>&1; do
    sleep 2
done
echo "[entrypoint] Ollama is ready."

# 检查模型是否已存在（volume 缓存）
if ollama list | grep -q "^${MODEL}"; then
    echo "[entrypoint] Model ${MODEL} already cached, skipping pull."
else
    echo "[entrypoint] Pulling model ${MODEL} (first boot, may take several minutes)..."
    ollama pull "${MODEL}"
    echo "[entrypoint] Model ${MODEL} ready."
fi

# 前台等待 Ollama 进程
wait $OLLAMA_PID
```

**关键点：**
- 使用 `ollama/ollama:latest` 官方镜像（内含 CUDA 支持）
- 模型数据挂载到 named volume，重启不重新下载（qwen2-vl:7b ≈ 4.7GB）
- HealthCheck 等待模型就绪后才放行 `depends_on`

---

### 2.3 markitdown API 服务

**文件：`docker/markitdown-api/app.py`**

整合 **REST API（FastAPI）** 和 **MCP 服务器（FastMCP）** 到单个进程，使用 Starlette 统一挂载。

```python
"""
MarkItDown 统一服务
同时提供：
  - REST API  (/api/convert, /api/health)     ← Electron 客户端调用
  - MCP SSE   (/sse, /messages/)              ← 旧版 MCP 客户端（如 Claude Desktop）
  - MCP HTTP  (/mcp)                          ← 新版 MCP 客户端（Cursor 等）
"""
import base64, contextlib, io, mimetypes, os
from collections.abc import AsyncIterator

from fastapi import FastAPI, File, HTTPException, UploadFile
from fastapi.middleware.cors import CORSMiddleware
from mcp.server import Server
from mcp.server.fastmcp import FastMCP
from mcp.server.sse import SseServerTransport
from mcp.server.streamable_http_manager import StreamableHTTPSessionManager
from markitdown import MarkItDown, StreamInfo
from markitdown_ocr import LLMVisionOCRService
from openai import OpenAI
from starlette.applications import Starlette
from starlette.requests import Request
from starlette.routing import Mount, Route
from starlette.types import Receive, Scope, Send
import uvicorn

# ── 环境变量 ──────────────────────────────────────────────────────
LLM_BASE_URL = os.getenv("LLM_BASE_URL", "http://ollama:11434/v1")
LLM_MODEL    = os.getenv("LLM_MODEL",    "qwen2-vl:7b")
LLM_API_KEY  = os.getenv("LLM_API_KEY",  "ollama")
LLM_PROMPT   = os.getenv("LLM_PROMPT",
    "请精确提取图片中所有文字，严格保持原始顺序和段落结构，只返回纯文字，不添加任何说明或标注。"
)

SUPPORTED_EXTENSIONS = {
    ".pdf", ".docx", ".doc", ".pptx", ".ppt",
    ".xlsx", ".xls", ".csv",
    ".jpg", ".jpeg", ".png",
    ".txt", ".md", ".html", ".epub",
}

# ── 共享工厂：构建带 LLM OCR 的 MarkItDown 实例 ──────────────────
def build_markitdown() -> MarkItDown:
    client = OpenAI(base_url=LLM_BASE_URL, api_key=LLM_API_KEY)
    ocr_service = LLMVisionOCRService(
        client=client, model=LLM_MODEL, default_prompt=LLM_PROMPT
    )
    return MarkItDown(enable_plugins=True, ocr_service=ocr_service)


def do_convert(filename: str, content: bytes) -> str:
    """共享转换逻辑，REST 和 MCP 工具均调用此函数。"""
    ext = os.path.splitext(filename)[1].lower()
    mime = mimetypes.guess_type(filename)[0] or "application/octet-stream"
    stream_info = StreamInfo(extension=ext, mimetype=mime, filename=filename)
    result = build_markitdown().convert_stream(io.BytesIO(content), stream_info=stream_info)
    return result.markdown


# ══════════════════════════════════════════════════════════════════
# Part A: REST API（FastAPI）
# ══════════════════════════════════════════════════════════════════
rest_app = FastAPI(title="MarkItDown REST API", version="1.0.0")
rest_app.add_middleware(
    CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"]
)

@rest_app.get("/health")
def health():
    return {"status": "ok", "llm_base_url": LLM_BASE_URL, "llm_model": LLM_MODEL}

@rest_app.post("/convert")
async def convert_file(file: UploadFile = File(...)):
    filename = file.filename or "upload"
    ext = os.path.splitext(filename)[1].lower()
    if ext not in SUPPORTED_EXTENSIONS:
        raise HTTPException(400, f"不支持的文件类型: {ext}")
    content = await file.read()
    try:
        markdown = do_convert(filename, content)
        return {"filename": filename, "markdown": markdown}
    except Exception as e:
        raise HTTPException(500, f"转换失败: {e}")


# ══════════════════════════════════════════════════════════════════
# Part B: MCP 服务器（FastMCP）
# ══════════════════════════════════════════════════════════════════
mcp = FastMCP("markitdown")

@mcp.tool()
async def convert_to_markdown(uri: str) -> str:
    """
    将 http:、https:、file: 或 data: URI 指向的资源转换为 Markdown。
    适合 LLM 直接传入网页链接或 data URI（base64 编码的文件）。
    """
    return build_markitdown().convert_uri(uri).markdown

@mcp.tool()
async def convert_file_content(filename: str, content_base64: str) -> str:
    """
    将 base64 编码的文件内容转换为 Markdown。
    参数：
      filename      - 原始文件名（含扩展名，用于判断格式），如 "report.pdf"
      content_base64 - 文件的 base64 编码字符串
    适合 LLM 调用时直接传入二进制文件内容。
    """
    ext = os.path.splitext(filename)[1].lower()
    if ext not in SUPPORTED_EXTENSIONS:
        return f"[错误] 不支持的文件类型: {ext}"
    try:
        content = base64.b64decode(content_base64)
        return do_convert(filename, content)
    except Exception as e:
        return f"[错误] 转换失败: {e}"


# ══════════════════════════════════════════════════════════════════
# Part C: Starlette 统一挂载
# ══════════════════════════════════════════════════════════════════
def build_app() -> Starlette:
    mcp_server: Server = mcp._mcp_server
    sse = SseServerTransport("/messages/")
    session_manager = StreamableHTTPSessionManager(
        app=mcp_server, event_store=None, json_response=True, stateless=True
    )

    async def handle_sse(request: Request) -> None:
        async with sse.connect_sse(
            request.scope, request.receive, request._send
        ) as (read_stream, write_stream):
            await mcp_server.run(
                read_stream, write_stream,
                mcp_server.create_initialization_options()
            )

    async def handle_streamable_http(scope: Scope, receive: Receive, send: Send):
        await session_manager.handle_request(scope, receive, send)

    @contextlib.asynccontextmanager
    async def lifespan(app: Starlette) -> AsyncIterator[None]:
        async with session_manager.run():
            yield

    return Starlette(
        routes=[
            Mount("/api",       app=rest_app),          # REST：/api/convert, /api/health
            Route("/sse",       endpoint=handle_sse),   # MCP SSE（旧版）
            Mount("/mcp",       app=handle_streamable_http),  # MCP HTTP（新版）
            Mount("/messages/", app=sse.handle_post_message), # MCP SSE 消息
        ],
        lifespan=lifespan,
    )


if __name__ == "__main__":
    uvicorn.run(build_app(), host="0.0.0.0", port=8778)
```

**文件：`docker/markitdown-api/Dockerfile`**

```dockerfile
FROM python:3.12-slim-bullseye

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y --no-install-recommends \
    tesseract-ocr \
    tesseract-ocr-chi-sim \
    tesseract-ocr-chi-tra \
    tesseract-ocr-eng \
    exiftool \
    ffmpeg \
    curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# 安装 markitdown 及全部插件依赖（含 llm + mcp）
COPY packages/ /app/packages/
RUN pip --no-cache-dir install \
    "/app/packages/markitdown[all]" \
    "/app/packages/markitdown-ocr[all]" \
    "/app/packages/markitdown-mcp" \
    "fastapi>=0.110.0" \
    "uvicorn[standard]>=0.29.0" \
    "python-multipart>=0.0.9"

# 复制统一服务入口
COPY docker/markitdown-api/app.py /app/app.py

EXPOSE 8778

HEALTHCHECK --interval=15s --timeout=5s --retries=3 \
    CMD curl -f http://localhost:8778/api/health || exit 1

CMD ["python", "app.py"]
```

---

### 2.4 docker-compose.yml

```yaml
# docker-compose.yml
name: markitdown

services:

  # ─── Ollama LLM 推理服务 ─────────────────────────────────────
  ollama:
    build:
      context: ./docker/ollama
    image: markitdown-ollama:latest
    container_name: markitdown-ollama
    restart: unless-stopped
    environment:
      OLLAMA_MODEL: qwen2-vl:7b
      OLLAMA_NUM_PARALLEL: 2         # 允许 2 个并发推理请求
      OLLAMA_MAX_LOADED_MODELS: 1
    volumes:
      - ollama-data:/root/.ollama    # 模型持久化，避免重复下载
    ports:
      - "11434:11434"               # 可选：对外暴露（调试用）
    networks:
      - markitdown-net
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      start_period: 180s            # 等待模型拉取（首次启动较慢）
      retries: 5

  # ─── MarkItDown REST API 服务 ────────────────────────────────
  markitdown-api:
    build:
      context: .
      dockerfile: docker/markitdown-api/Dockerfile
    image: markitdown-api:latest
    container_name: markitdown-api
    restart: unless-stopped
    environment:
      LLM_BASE_URL: http://ollama:11434/v1
      LLM_MODEL: qwen2-vl:7b
      LLM_API_KEY: ollama
      LLM_PROMPT: "请精确提取图片中所有文字，严格保持原始顺序和段落结构，只返回纯文字。"
    ports:
      - "8778:8778"                 # Electron 客户端连接此端口
    networks:
      - markitdown-net
    depends_on:
      ollama:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8778/health"]
      interval: 15s
      timeout: 5s
      retries: 3

networks:
  markitdown-net:
    driver: bridge

volumes:
  ollama-data:
    name: markitdown-ollama-models
```

---

### 2.5 启动与验证

```bash
# 首次启动（会自动拉取 qwen2-vl:7b，约 4.7GB，需等待）
docker compose up -d

# 查看日志（监控模型拉取进度）
docker compose logs -f ollama

# 验证 REST API
curl http://localhost:8778/api/health
# → {"status":"ok","llm_base_url":"http://ollama:11434/v1","llm_model":"qwen2-vl:7b"}

# 测试图片转换
curl -X POST http://localhost:8778/api/convert \
  -F "file=@test/222.png" | jq .markdown

# 验证 MCP 服务（查看工具列表）
curl -N http://localhost:8778/sse
```

---

### 2.6 MCP 接入能力

#### 端点说明

| 端点 | 协议 | 适用客户端 |
|------|------|-----------|
| `GET /sse` + `POST /messages/` | MCP over SSE | Claude Desktop、旧版 MCP 客户端 |
| `POST /mcp` | MCP StreamableHTTP | Cursor、新版 MCP 客户端 |
| `POST /api/convert` | HTTP REST | Electron 客户端 |
| `GET /api/health` | HTTP REST | 健康检查 / 监控 |

#### MCP 工具列表

| 工具名 | 参数 | 说明 |
|--------|------|------|
| `convert_to_markdown` | `uri: str` | 转换 http/https/file/data URI 资源 |
| `convert_file_content` | `filename: str`, `content_base64: str` | 转换 base64 编码的二进制文件 |

#### 接入 Claude Desktop（SSE）

在 Claude Desktop 配置文件（`claude_desktop_config.json`）中添加：

```json
{
  "mcpServers": {
    "markitdown": {
      "url": "http://localhost:8778/sse"
    }
  }
}
```

> 配置后，Claude 即可直接调用 `convert_to_markdown` 工具处理文档。

#### 接入 Cursor（StreamableHTTP）

在 Cursor 设置 → MCP Servers 中添加：

```json
{
  "markitdown": {
    "url": "http://localhost:8778/mcp"
  }
}
```

#### LLM 调用示例（data URI 模式）

对于无法直接访问文件系统的 LLM，可将文件 base64 编码后通过 `data:` URI 传入：

```python
import base64

with open("report.pdf", "rb") as f:
    b64 = base64.b64encode(f.read()).decode()

# LLM 调用 convert_to_markdown 工具时传入：
uri = f"data:application/pdf;base64,{b64}"
# → convert_to_markdown(uri=uri)
```

或使用 `convert_file_content` 工具：

```python
# LLM 调用：
# convert_file_content(filename="report.pdf", content_base64=b64)
```

---

## 三、Part 2：Electron 桌面客户端

### 3.1 目录结构规划

```
electron/
├── package.json
├── main.js                # 主进程：窗口管理 + IPC
├── preload.js             # 预加载：向渲染层暴露 API
├── icon.png / icon.svg
├── renderer/
│   ├── index.html         # 主界面
│   ├── styles.css         # 样式
│   └── app.js             # 渲染进程逻辑
└── scripts/
    ├── build-icon.js
    └── copy-static.js     # 与 voice_assistant 相同的构建脚本
```

---

### 3.2 UI 设计方案

参考 `voice_assistant/electron` 的简洁风格，采用**原生 HTML/CSS/JS**（无框架，便于打包和维护）。

#### 界面布局

```
┌─────────────────────────────────────────────────────┐
│  MarkItDown 文档转换                          [设置]  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │                                             │   │
│  │         📄 拖拽文件到此处                   │   │
│  │         或点击选择文件                      │   │
│  │                                             │   │
│  │   支持：PDF · DOCX · PPTX · XLSX · JPG · PNG│   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ─────────────────── 转换结果 ─────────────────────  │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ # 提取文字内容                               │   │
│  │                                             │   │
│  │ 高时效处置与复核，提高任务执行自动化程度...   │   │
│  │                                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  [📋 复制 Markdown]  [💾 保存文件]   转换耗时: 3.2s │
└─────────────────────────────────────────────────────┘
```

#### 关键交互设计
- **拖拽上传**：拖拽文件到区域自动上传
- **点击选择**：`dialog.showOpenDialog` 打开系统文件选择器
- **实时状态**：上传中显示进度条，错误时显示红色提示
- **结果展示**：纯文本 Markdown 预览（可滚动）
- **一键操作**：复制到剪贴板 / 保存为 `.md` 文件
- **设置入口**：可修改服务端 URL（默认 `http://localhost:8778`）

---

### 3.3 主进程 main.js 设计

```javascript
const { app, BrowserWindow, ipcMain, dialog, shell, clipboard } = require('electron');
const fs = require('fs');
const path = require('path');
const FormData = require('form-data');     // npm: form-data
const fetch = require('node-fetch');       // npm: node-fetch@2（CommonJS）

// 窗口配置
const WIN_CONFIG = {
  width: 860,
  height: 700,
  minWidth: 600,
  minHeight: 500,
  title: 'MarkItDown 文档转换',
  autoHideMenuBar: true,
  webPreferences: {
    preload: path.join(__dirname, 'preload.js'),
    contextIsolation: true,
    nodeIntegration: false,
  },
};

// IPC 处理器注册
function registerIPC() {
  // 1. 打开文件选择对话框
  ipcMain.handle('dialog:open-file', async () => {
    const { filePaths } = await dialog.showOpenDialog({
      filters: [
        { name: '支持的文件', extensions: ['pdf','docx','pptx','xlsx','jpg','jpeg','png','csv','txt','md'] },
      ],
      properties: ['openFile'],
    });
    return filePaths[0] || null;
  });

  // 2. 发送文件到 API 并返回 Markdown
  ipcMain.handle('convert:file', async (_, { filePath, apiUrl }) => {
    const form = new FormData();
    form.append('file', fs.createReadStream(filePath));
    const res = await fetch(`${apiUrl}/convert`, { method: 'POST', body: form });
    if (!res.ok) throw new Error(`API Error ${res.status}: ${await res.text()}`);
    return await res.json();  // { filename, markdown }
  });

  // 3. 保存 Markdown 文件
  ipcMain.handle('dialog:save-file', async (_, { defaultName, content }) => {
    const { filePath } = await dialog.showSaveDialog({
      defaultPath: defaultName.replace(/\.[^.]+$/, '.md'),
      filters: [{ name: 'Markdown', extensions: ['md'] }],
    });
    if (filePath) {
      fs.writeFileSync(filePath, content, 'utf-8');
      return filePath;
    }
    return null;
  });

  // 4. 复制到剪贴板
  ipcMain.handle('clipboard:write', (_, text) => {
    clipboard.writeText(text);
  });
}
```

---

### 3.4 preload.js 设计

```javascript
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('markitdownAPI', {
  isElectron: true,
  defaultApiUrl: 'http://localhost:8778',

  openFile: () => ipcRenderer.invoke('dialog:open-file'),
  convertFile: (filePath, apiUrl) => ipcRenderer.invoke('convert:file', { filePath, apiUrl }),
  saveFile: (defaultName, content) => ipcRenderer.invoke('dialog:save-file', { defaultName, content }),
  copyToClipboard: (text) => ipcRenderer.invoke('clipboard:write', text),
});
```

---

### 3.5 renderer/app.js 核心逻辑

```javascript
// 核心流程：文件 → API → Markdown 展示
async function convertFile(filePath) {
  const apiUrl = getSavedApiUrl();  // localStorage 读取，默认 localhost:8778

  setStatus('uploading');
  const startTime = Date.now();

  try {
    const result = await window.markitdownAPI.convertFile(filePath, apiUrl);
    const elapsed = ((Date.now() - startTime) / 1000).toFixed(1);

    showResult(result.markdown, result.filename, elapsed);
    setStatus('done');
  } catch (err) {
    setStatus('error', err.message);
  }
}
```

---

### 3.6 package.json

```json
{
  "name": "markitdown-desktop",
  "version": "1.0.0",
  "description": "MarkItDown 文档转 Markdown 桌面客户端",
  "main": "main.js",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build:linux": "electron-builder --linux",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac"
  },
  "dependencies": {
    "form-data": "^4.0.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "sharp": "^0.33.0"
  },
  "build": {
    "appId": "com.markitdown.desktop",
    "productName": "MarkItDown",
    "icon": "icon.png",
    "directories": { "output": "dist" },
    "files": ["main.js", "preload.js", "icon.png", "renderer/**/*"],
    "linux":  { "target": ["AppImage", "deb"], "category": "Utility" },
    "win":    { "target": ["nsis", "portable"] },
    "mac":    { "target": ["dmg"] }
  }
}
```

---

## 四、需要新增/修改的代码清单

### 需新增（共 11 个文件）

| 文件路径 | 类型 | 行数估计 | 说明 |
|---------|------|---------|------|
| `docker/ollama/Dockerfile` | 新增 | ~15 | Ollama 服务镜像 |
| `docker/ollama/entrypoint.sh` | 新增 | ~30 | 启动脚本 + 自动拉取模型 |
| `docker/markitdown-api/Dockerfile` | 新增 | ~25 | markitdown API 服务镜像 |
| `docker/markitdown-api/app.py` | 新增 | ~120 | REST + MCP 统一服务 |
| `docker-compose.yml` | 新增 | ~60 | 服务编排 |
| `electron/main.js` | 新增 | ~80 | Electron 主进程 |
| `electron/preload.js` | 新增 | ~15 | 预加载脚本 |
| `electron/package.json` | 新增 | ~50 | 包配置 |
| `electron/renderer/index.html` | 新增 | ~80 | 主界面 |
| `electron/renderer/styles.css` | 新增 | ~120 | 样式 |
| `electron/renderer/app.js` | 新增 | ~100 | 渲染进程逻辑 |

### 无需修改现有代码

| 文件 | 原因 |
|------|------|
| `Dockerfile` | 保留原有 Tesseract CLI 版本不变 |
| `packages/markitdown-ocr/_ocr_service.py` | `LLMVisionOCRService` 已支持自定义 client |
| `packages/markitdown-ocr/_plugin.py` | `app.py` 直接调用 Python API，绕过 CLI |
| `packages/markitdown/__main__.py` | API 服务不使用 CLI |
| `packages/markitdown-mcp/__main__.py` | 直接复用其 MCP 路由构建逻辑，不修改原文件 |

> **关键决策**：`app.py` 同时 import `FastAPI`（REST）和 `FastMCP`（MCP），
> 通过 Starlette 统一挂载到同一端口，无需拆分进程，也无需修改任何现有代码。

---

## 五、实施路径与工作量估算

```
Week 1：Docker 服务栈
  Day 1-2：编写 Ollama Dockerfile + entrypoint.sh，本地测试 GPU 推理
  Day 3：编写 markitdown-api/app.py + Dockerfile
  Day 4：编写 docker-compose.yml，联调两个服务
  Day 5：端到端测试（用 curl 测试图片/PDF 转换质量）

Week 2：Electron 桌面客户端
  Day 1：搭建项目骨架（参考 voice_assistant/electron）
  Day 2-3：实现文件上传 + API 调用 + Markdown 展示
  Day 4：UI 细化（拖拽、进度、错误处理、设置页）
  Day 5：多平台打包测试（Linux AppImage / Windows NSIS）
```

---

## 六、风险与注意事项

### 6.1 模型首次拉取
- qwen2-vl:7b 约 **4.7GB**，首次 `docker compose up` 需下载
- 建议生产部署时预先 `docker pull`，或使用本地模型文件挂载
- 使用 `docker compose logs -f ollama` 监控拉取进度

### 6.2 GPU 配置
- 宿主机需安装 **nvidia-container-toolkit**
- WSL2 已有 V100 16GB VRAM，qwen2-vl:7b FP16 约占用 15GB，可正常运行
- 若 VRAM 不足，改用 `qwen2-vl:7b-q4_K_M`（约 5GB）

```bash
# 安装 nvidia-container-toolkit（若未安装）
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list \
  | sudo tee /etc/apt/sources.list.d/nvidia-docker.list
sudo apt-get update && sudo apt-get install -y nvidia-container-toolkit
sudo systemctl restart docker
```

### 6.3 Electron 跨平台文件上传
- Windows 路径包含反斜杠，需使用 `path.resolve()` 统一处理
- Linux 打包为 AppImage 需宿主安装 `libfuse2`
- macOS 打包需签名（否则提示"无法验证开发者"）

### 6.4 API 安全
- 默认仅监听 `localhost:8778`，不对外暴露
- 如部署远程服务器，需在 markitdown-api 前加 Nginx + Basic Auth 或 Token 验证
- 文件大小限制：FastAPI 默认无限，建议加 `--limit-max-requests` 或 Nginx 限制

### 6.5 推理性能参考（V100 16GB）

| 文件类型 | 预期耗时 |
|---------|---------|
| 普通图片（1-2 列文字） | 2-5 秒 |
| 复杂版面（表格/多列） | 5-10 秒 |
| PDF（每页图片化） | 3-8 秒/页 |
| DOCX（含嵌入图片） | 5-15 秒 |

---

## 七、验收标准

### Docker 服务栈
- [ ] `docker compose up` 一键启动，首次自动下载模型
- [ ] `curl -F "file=@test/222.png" http://localhost:8778/api/convert` 返回准确中文文字
- [ ] `curl http://localhost:8778/api/health` 返回 ok

### MCP 接入
- [ ] `GET /sse` 返回 SSE 事件流（工具列表包含 2 个工具）
- [ ] `POST /mcp` 返回有效 MCP 响应
- [ ] Claude Desktop 可通过 SSE 地址发现并调用 `convert_to_markdown` 工具
- [ ] Cursor 可通过 StreamableHTTP 地址发现并调用工具
- [ ] `convert_file_content` 工具正确处理 base64 编码的 PNG/PDF

### Electron 桌面客户端
- [ ] Electron 应用可在 Linux 上运行并正确显示 Markdown 预览
- [ ] 拖拽 / 点击两种上传方式均可用
- [ ] 复制和保存功能正常
- [ ] 服务端 URL 可通过设置界面修改并持久化
- [ ] Windows / macOS 安装包可正常安装运行
